[project]
name = "x-tf-gridnet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hdf5storage>=0.2.2",
    "soundfile>=0.13.1",
    "torch>=2.11.0",
]
